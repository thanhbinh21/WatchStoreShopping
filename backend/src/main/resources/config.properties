# AI provider (OpenAI) configuration
# NOTE: Do NOT commit API secrets to source control.
# If you previously added a secret here, delete it and rotate your key in the OpenAI Dashboard.
# Keep the property for reference only and prefer using the environment variable OPENAI_API_KEY instead.
#openai.api.key=${OPENAI_API_KEY:}
