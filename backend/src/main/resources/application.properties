spring.application.name=Nhom08_WWW_JAVA

# ===============================
# DATABASE
# ===============================
spring.datasource.url=jdbc:mariadb://localhost:3306/watch_store
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# ===============================
# JPA / HIBERNATE
# ===============================
# T? ??ng c?p nh?t schema (create, create-drop, update, validate)
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.hibernate.ddl-auto=create

# In c�u SQL ra console
#spring.jpa.show-sql=true

# Format SQL cho d? ??c
spring.jpa.properties.hibernate.format_sql=true

# Dialect cho MariaDB
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# ===============================
# SERVER
# ===============================
# Port c?a Spring Boot (m?c ??nh 8080)
server.port=8080

# ===============================
# EMAIL (SMTP)

#spring.mail.host=smtp.gmail.com
#spring.mail.port=587
#spring.mail.username=${MAIL_USERNAME}
#spring.mail.password=${MAIL_PASSWORD}
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true
#
#spring.mail.test-connection=false
#
# --- Alternative: Mailtrap (development) ---
spring.mail.host=smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=08c668f74bf006
spring.mail.password=f6ef8168040e35
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Frontend base URL used in password reset links
app.frontend.url=http://localhost:5173

# ===============================
# Groq / AI (OpenAI-compatible)
# ===============================
# The backend reads `groq.api.key` from the environment variable `GROQ_API_KEY`.
# Do not commit secrets in the repository. Example:
#  - Linux/macOS: export GROQ_API_KEY="gsk_..."
#  - Windows PowerShell: $env:GROQ_API_KEY = "gsk_..."
# The property defaults to empty if not provided.
# ===============================
# Groq / AI
# ===============================

# VNPay Configuration
vnpay.tmn-code=AQBWQHNF
vnpay.secret-key=YDNFZCHHRCG4XH4UUDE1IHF4T4Q7T5FY
vnpay.return-url=http://localhost:8080/api/payment/vnpay-return
vnpay.pay-url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
